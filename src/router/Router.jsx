import React, { Fragment } from 'react';
import * as Pages from '../pages';

const Router = () => (
  <Fragment>
    {/* 리액트 라우터가 들어가야 될 부분 */}
    <Pages.Home />
  </Fragment>
);

export default Router;
